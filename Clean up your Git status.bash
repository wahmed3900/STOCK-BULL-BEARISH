# Check what's changed
git status

# Add only the files you want to commit
git add app.py "Refactored Code (DRY Principle).py"

# Commit with a clear message
git commit -m "Refactor: Clean up stock validation app with DRY principles"